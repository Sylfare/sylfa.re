---
const { slug, name, checked } = Astro.props;
---
<div id="field-row">
    <input type="radio" id={"theme-"+slug} name="theme" data-theme={slug} {checked} />
    <label for={"theme-"+slug}>{name}</label>
</div>


